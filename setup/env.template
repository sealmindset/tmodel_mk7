# TModel MK7 Environment Configuration

# Application Settings
PORT=3000
NODE_ENV=development
SESSION_SECRET=replace_with_random_secret

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# LLM Provider Settings
# Set to 'openai' or 'ollama' to use the respective provider
LLM_PROVIDER=ollama

# OpenAI API Configuration (if using OpenAI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4

# Ollama Configuration (if using Ollama)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3

# Rapid7 API Configuration (for vulnerability management)
RAPID7_API_URL=http://localhost:3100
RAPID7_API_KEY=mock-api-key
